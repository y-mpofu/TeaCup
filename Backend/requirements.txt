# === Core Web Framework ===
fastapi>=0.104.1                # Main backend framework for building APIs
uvicorn[standard]>=0.24.0       # ASGI server to serve FastAPI (includes uvloop)

# === Environment and Config ===
python-dotenv>=1.0.0            # Loads .env for API keys and environment variables

# === HTTP, CORS, and File Uploads ===
requests>=2.31.0                # For synchronous external API calls
aiohttp>=3.9.0                  # For async HTTP requests (Google API, web scraping)
python-multipart>=0.0.6         # File upload/form-data support

# === Data Validation & Typing ===
pydantic>=2.5.0                 # FastAPI uses this for schema validation
typing-extensions>=4.8.0        # Enables extended type hints

# === Web Scraping Tools ===
beautifulsoup4>=4.12.0          # HTML parsing
lxml>=4.9.3                     # Fast XML/HTML parser for BeautifulSoup
html5lib>=1.1                   # HTML parser (fallback for BeautifulSoup)
newspaper3k>=0.2.8              # Lightweight article extraction
feedparser>=6.0.0               # RSS feed parsing


# === Google Search / API Integration ===
google-api-python-client>=2.80.0
google-auth>=2.16.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# === Firebase Integration ===

# === Utility Libraries ===
python-dateutil>=2.8.0         # Better datetime parsing
pandas>=2.0.0                   # Dataframes & analysis (for news enrichment)

# === Testing & Dev Tools ===
pytest>=7.0.0                   # Testing framework

# === Optional for Production ===
# gunicorn>=21.2.0             # Production WSGI server (not required for ASGI/FastAPI)




# Backend/requirements.txt
# Python dependencies for the TeaCup News backend

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Web Scraping Dependencies
beautifulsoup4==4.12.2        # HTML parsing and scraping
aiohttp==3.9.1                 # Async HTTP client for fetching web pages
lxml==4.9.3                    # Fast XML/HTML parser (optional but recommended for BeautifulSoup)
html5lib==1.1                  # Alternative HTML parser for complex pages
requests==2.31.0               # Synchronous HTTP client (backup option)

# AI and NLP
openai==1.3.5                  # OpenAI GPT API client
tiktoken==0.5.1                # Token counting for OpenAI models

# Authentication and Security
firebase-admin==6.1.0          # Firebase authentication
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4        # Password hashing
python-dotenv==1.0.0           # Environment variable management

# Database (if needed later)
# sqlalchemy==2.0.23
# asyncpg==0.29.0              # PostgreSQL async driver

# Data Validation and Serialization
pydantic==2.5.0                # Data validation
pydantic-settings==2.1.0       # Settings management

# Utilities
python-dateutil==2.8.2         # Date parsing utilities
pytz==2023.3                   # Timezone handling
regex==2023.10.3               # Advanced regex support

# Development and Testing
pytest==7.4.3                  # Testing framework
pytest-asyncio==0.21.1         # Async test support
httpx==0.25.1                  # Test client for FastAPI
black==23.11.0                 # Code formatting
flake8==6.1.0                  # Linting

# Logging and Monitoring
loguru==0.7.2                  # Advanced logging

# Optional: News-specific libraries
newspaper3k==0.2.8             # Alternative article extraction (optional)
# feedparser==6.0.10           # RSS feed parsing (if needed)

# CORS Support
fastapi-cors==0.0.6           # CORS middleware

# Rate Limiting (optional but recommended)
slowapi==0.1.9                 # Rate limiting for FastAPI

# Installation Instructions:
# 1. Create a virtual environment:
#    python -m venv venv
#
# 2. Activate the virtual environment:
#    - Windows: venv\Scripts\activate
#    - Mac/Linux: source venv/bin/activate
#
# 3. Install dependencies:
#    pip install -r requirements.txt
#
# 4. For development only, you might also want:
#    pip install ipython jupyter