# Core Web Framework (Essential - install these first)
fastapi>=0.100.0         # Main backend framework for building APIs
uvicorn>=0.20.0          # ASGI server to run FastAPI (like a waiter serving your API)

# Environment and Configuration
python-dotenv>=1.0.0     # Loads your .env file with API keys safely

# HTTP Requests and API calls
requests>=2.28.0         # For making calls to Google Search API, Bing API, OpenAI
httpx>=0.24.0            # Alternative HTTP client (good for async operations)

# Web Scraping (simpler versions)
beautifulsoup4>=4.11.0   # For crawling and parsing websites
html5lib>=1.1            # HTML parser (more compatible than lxml)

# AI/ML Libraries  
openai>=1.0.0           # Official OpenAI library for GPT-3.5 turbo API calls

# Google APIs (for search functionality)
google-api-python-client>=2.80.0  # Main Google API client library
google-auth>=2.16.0               # Authentication for Google APIs
google-auth-oauthlib>=1.0.0       # OAuth authentication flow
google-auth-httplib2>=0.2.0       # HTTP transport for Google Auth

# Database (Firebase)
firebase-admin>=6.0.0     # Official Firebase SDK for Python
google-cloud-firestore>=2.10.0  # Firestore database operations

# Security and Validation (FastAPI dependency)
pydantic>=2.0.0         # Data validation (FastAPI uses this)

# CORS (Cross-Origin Resource Sharing)
python-multipart>=0.0.5 # For handling file uploads in FastAPI

# Optional - Add later once core is working
pandas>=2.0.0            # For organizing and analyzing news data (helpful for data processing)
nltk>=3.8.0              # Natural language processing (for text analysis)
pytest>=7.0.0           # Testing framework

# Additional useful packages for news processing
feedparser>=6.0.0        # For parsing RSS feeds from news websites
newspaper3k>=0.2.8       # Alternative news scraping library (backup to Beautiful Soup)
python-dateutil>=2.8.0   # Better date/time handling for news timestamps